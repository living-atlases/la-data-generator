diff --git a/ansible/roles/doi-service/tasks/main.yml b/ansible/roles/doi-service/tasks/main.yml
index 86e2ce22..0d77bbc4 100644
--- a/ansible/roles/doi-service/tasks/main.yml
+++ b/ansible/roles/doi-service/tasks/main.yml
@@ -78,6 +78,7 @@
     enabled: yes
   tags:
     - doi-service
+    - restart
 
 - name: add nginx vhost
   include_role:
diff --git a/ansible/roles/image-service/handlers/main.yml b/ansible/roles/image-service/handlers/main.yml
index ed866b7f..93aa3161 100644
--- a/ansible/roles/image-service/handlers/main.yml
+++ b/ansible/roles/image-service/handlers/main.yml
@@ -1,2 +1,2 @@
 - name: restart image-service
-  service: name=image-service state=restarted enabled=yes
+  service: name=image-service state=restarted enabled=no
diff --git a/ansible/roles/image-service/tasks/main.yml b/ansible/roles/image-service/tasks/main.yml
index 27e0f3fe..a4584312 100644
--- a/ansible/roles/image-service/tasks/main.yml
+++ b/ansible/roles/image-service/tasks/main.yml
@@ -77,8 +77,8 @@
   file: path={{item}} state=directory
   with_items:
     - "/srv/{{ image_service_hostname }}/www"
-  notify:
-     - restart image-service
+#  notify:
+#     - restart image-service
   tags:
     - image-service
 
@@ -92,8 +92,8 @@
     - "{{data_dir}}/image-service/exports"
     - "{{data_dir}}/image-service/bin/imgcnv"
     - "/var/log/atlas/image-service"
-  notify:
-     - restart image-service
+#  notify:
+#     - restart image-service
   tags:
     - image-service
 
@@ -110,8 +110,8 @@
   with_items:
     - "config/image-service-config.yml"
     - "config/logback.groovy"
-  notify:
-     - restart image-service
+#  notify:
+#     - restart image-service
   tags:
     - deploy
     - image-service
@@ -121,8 +121,8 @@
   template: src={{ item }} dest={{data_dir}}/image-service/setup/
   with_items:
     - "sql/image-service-export.sql"
-  notify:
-     - restart image-service
+#  notify:
+#     - restart image-service
   tags:
     - deploy
     - image-service
@@ -132,10 +132,11 @@
   command: "psql -d {{ image_service_db_username }} -f {{data_dir}}/image-service/setup/image-service-export.sql"
   become_user: postgres
   ignore_errors: no
-  notify:
-     - restart image-service
+#  notify:
+#     - restart image-service
   tags:
     - image-service
+    - image-stored-procedures
     - properties
     - db
 
@@ -160,15 +161,15 @@
     - "{{data_dir}}/image-service/exports"
     - "{{data_dir}}/image-service/bin/imgcnv"
     - "/var/log/atlas/image-service"
-  notify:
-     - restart image-service
+#  notify:
+#     - restart image-service
   tags:
     - image-service
 
 - name: set data ownership for {{data_dir}}/image-service
   file: path={{data_dir}}/image-service owner="image-service" group="image-service"
-  notify:
-     - restart image-service
+#  notify:
+#     - restart image-service
   tags:
     - image-service
     - properties
@@ -176,8 +177,8 @@
 
 - name: set data ownership postgres for {{data_dir}}/image-service/exports
   file: path={{data_dir}}/image-service/exports owner="postgres" group="postgres" recurse=true
-  notify:
-     - restart image-service
+#  notify:
+#     - restart image-service
   tags:
     - image-service
     - properties
@@ -185,8 +186,8 @@
 
 - name: set data ownership for /opt/atlas/image-service
   file: path=/opt/atlas/image-service owner="image-service" group="image-service" recurse=true
-  notify:
-     - restart image-service
+#  notify:
+#     - restart image-service
   tags:
     - image-service
     - properties
@@ -194,8 +195,8 @@
 
 - name: set data ownership for /var/log/atlas/image-service
   file: path=/var/log/atlas/image-service owner="image-service" group="image-service" recurse=true
-  notify:
-     - restart image-service
+#  notify:
+#     - restart image-service
   tags:
     - image-service
     - properties
@@ -204,8 +205,8 @@
 - name: install openjfx (Debian)
   apt: pkg=openjfx state=latest update_cache=yes
   when: ansible_os_family == "Debian" and use_openjdk8
-  notify:
-     - restart image-service
+#  notify:
+#     - restart image-service
   tags:
     - packages
     - image-service
diff --git a/ansible/roles/tomcat/handlers/main.yml b/ansible/roles/tomcat/handlers/main.yml
index 6cbd09a4..23235507 100644
--- a/ansible/roles/tomcat/handlers/main.yml
+++ b/ansible/roles/tomcat/handlers/main.yml
@@ -2,7 +2,7 @@
 
 # variable controls version (@see common/vars)
 - name: restart tomcat
-  service: name={{tomcat}} state=restarted enabled=yes
+  service: name={{tomcat}} state=restarted enabled=no
 
 - name: restart httpd
   service: name={{apache}} state=restarted enabled=yes
